import React from "react";
import PaintingPresenter from "./PaintingPresenter";

export default class extends React.Component {
    render() {
        return (
            <PaintingPresenter />
        )
    }
}